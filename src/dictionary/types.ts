export type Dictionary = {
  traditional: string[];
  simplified: string[];
};

export type DictionaryEntry = {
  traditional: string;
  simplified: string;
  pronunciation: string;
  meaning: string;
};
